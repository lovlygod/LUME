п»ї??# LUME Profile UI

**пїЅпїЅпїЅпїЅпїЅпїЅпїЅпїЅпїЅ пїЅпїЅпїЅпїЅпїЅпїЅпїЅпїЅпїЅпїЅ:** 9 пїЅпїЅпїЅпїЅпїЅ 2026 пїЅ.

---

## РћР±Р·РѕСЂ

РџСЂРѕС„РёР»СЊ РїРѕР»СЊР·РѕРІР°С‚РµР»СЏ вЂ” СЌС‚Рѕ СЃС‚СЂР°РЅРёС†Р° СЃ РёРЅС„РѕСЂРјР°С†РёРµР№ Рѕ РїРѕР»СЊР·РѕРІР°С‚РµР»Рµ, РµРіРѕ РїСѓР±Р»РёРєР°С†РёСЏРјРё Рё СЃС‚Р°С‚РёСЃС‚РёРєРѕР№.

**Р¤Р°Р№Р»С‹:**
- РњРѕР№ РїСЂРѕС„РёР»СЊ: `src/pages/Profile.tsx`
- Р§СѓР¶РѕР№ РїСЂРѕС„РёР»СЊ: `src/pages/UserProfile.tsx`
- РљРѕРјРїРѕРЅРµРЅС‚С‹: `src/components/profile/`

---

## РЎС‚СЂСѓРєС‚СѓСЂР° СЃС‚СЂР°РЅРёС†С‹

```
в”Њв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”ђ
в”‚  Banner Image (1200x300)                            в”‚
в”‚  в”Њв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”ђ  в”‚
в”‚  в”‚                                               в”‚  в”‚
в”‚  в”‚    [Avatar]                                   в”‚  в”‚
в”‚  в”‚                                               в”‚  в”‚
в”‚  в””в”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”  в”‚
в”‚                                                     в”‚
в”‚  Name [Verified]                                    в”‚
в”‚  @username                                          в”‚
в”‚                                                     в”‚
в”‚  Bio description here...                            в”‚
в”‚                                                     в”‚
в”‚  рџ“Ќ City  В·  рџ”— Website  В·  рџ“… Joined Jan 2024     в”‚
в”‚                                                     в”‚
в”‚  1.2K Followers  В·  543 Following  В·  89 Posts     в”‚
в”‚                                                     в”‚
в”‚  [Edit Profile] [Message]  (РёР»Рё [Follow] [Message])в”‚
в”‚  в”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђ  в”‚
в”‚                                                     в”‚
в”‚  Tabs: [Posts] [Media] [Likes]                      в”‚
в”‚  в”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђ  в”‚
в”‚                                                     в”‚
в”‚  Pinned Post (РµСЃР»Рё РµСЃС‚СЊ)                            в”‚
в”‚  в”Њв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”ђ  в”‚
в”‚  в”‚ рџ“Њ Pinned                                     в”‚  в”‚
в”‚  в”‚ [Post content...]                             в”‚  в”‚
в”‚  в””в”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”  в”‚
в”‚                                                     в”‚
в”‚  Posts Feed                                         в”‚
в”‚  в”Њв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”ђ  в”‚
в”‚  в”‚ [Post 1]                                      в”‚  в”‚
в”‚  в”њв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”¤  в”‚
в”‚  в”‚ [Post 2]                                      в”‚  в”‚
в”‚  в”њв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”¤  в”‚
в”‚  в”‚ [Post 3]                                      в”‚  в”‚
в”‚  в””в”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”  в”‚
в””в”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”Ђв”
```

---

## Profile Header

**Р¤Р°Р№Р»:** `src/pages/Profile.tsx`, `UserProfile.tsx`

### Banner

```tsx
<div className="relative h-48 w-full overflow-hidden bg-gradient-to-br from-purple-500/20 to-blue-500/20">
  {user.banner ? (
    <img
      src={user.banner}
      alt="Profile banner"
      className="w-full h-full object-cover"
    />
  ) : (
    <div className="w-full h-full bg-gradient-to-br from-purple-500/20 to-blue-500/20" />
  )}
</div>
```

### Avatar

```tsx
<div className="relative -mt-16 mb-4 px-6">
  <div className="relative inline-block">
    <Avatar
      src={user.avatar}
      fallback={user.name.charAt(0)}
      className="w-32 h-32 border-4 border-background"
    />
    {isOwn && (
      <label className="absolute bottom-0 right-0 p-2 rounded-full bg-primary cursor-pointer hover:bg-primary/90">
        <CameraIcon className="h-5 w-5" />
        <input
          type="file"
          accept="image/*"
          className="hidden"
          onChange={handleAvatarChange}
        />
      </label>
    )}
  </div>
</div>
```

### User Info

```tsx
<div className="px-6 mb-6">
  {/* Name */}
  <div className="flex items-center gap-2 mb-1">
    <h1 className="text-2xl font-bold">{user.name}</h1>
    {user.verified && <VerifiedBadge className="h-6 w-6" />}
  </div>

  {/* Username */}
  <p className="text-secondary">@{user.username}</p>

  {/* Bio */}
  {user.bio && (
    <p className="mt-3 text-base">{user.bio}</p>
  )}

  {/* Meta info */}
  <div className="flex items-center gap-4 mt-3 text-sm text-secondary">
    {user.city && (
      <div className="flex items-center gap-1">
        <MapPinIcon className="h-4 w-4" />
        <span>{user.city}</span>
      </div>
    )}
    {user.website && (
      <a
        href={user.website}
        target="_blank"
        rel="noopener noreferrer"
        className="flex items-center gap-1 hover:text-primary"
      >
        <LinkIcon className="h-4 w-4" />
        <span className="truncate">{formatUrl(user.website)}</span>
      </a>
    )}
    <div className="flex items-center gap-1">
      <CalendarIcon className="h-4 w-4" />
      <span>Joined {formatDate(user.joinDate)}</span>
    </div>
  </div>

  {/* Stats */}
  <div className="flex items-center gap-4 mt-4">
    <button
      className="hover:underline"
      onClick={() => openFollowModal('followers')}
    >
      <span className="font-semibold">{formatNumber(user.followersCount)}</span>
      <span className="text-secondary ml-1">Followers</span>
    </button>
    <button
      className="hover:underline"
      onClick={() => openFollowModal('following')}
    >
      <span className="font-semibold">{formatNumber(user.followingCount)}</span>
      <span className="text-secondary ml-1">Following</span>
    </button>
    <div>
      <span className="font-semibold">{formatNumber(user.postsCount)}</span>
      <span className="text-secondary ml-1">Posts</span>
    </div>
  </div>
</div>
```

### Action Buttons

**РњРѕР№ РїСЂРѕС„РёР»СЊ:**
```tsx
<div className="px-6 pb-6 flex gap-3">
  <Button onClick={() => navigate('/settings')} variant="outline">
    <SettingsIcon className="h-4 w-4 mr-2" />
    Settings
  </Button>
  <Button onClick={() => setIsEditOpen(true)}>
    Edit Profile
  </Button>
</div>
```

**Р§СѓР¶РѕР№ РїСЂРѕС„РёР»СЊ:**
```tsx
<div className="px-6 pb-6 flex gap-3">
  {isFollowing ? (
    <>
      <Button variant="outline" onClick={handleUnfollow}>
        Unfollow
      </Button>
      <Button variant="outline" onClick={() => navigate(`/messages/${user.id}`)}>
        Message
      </Button>
    </>
  ) : (
    <>
      <Button variant="outline" onClick={handleFollow}>
        Follow
      </Button>
      <Button onClick={() => navigate(`/messages/${user.id}`)}>
        Message
      </Button>
    </>
  )}
</div>
```

---

## Edit Profile Modal

**Р¤Р°Р№Р»:** `src/pages/Profile.tsx`

### РЎС‚СЂСѓРєС‚СѓСЂР°

```tsx
<Dialog open={isEditOpen} onOpenChange={setIsEditOpen}>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>Edit Profile</DialogTitle>
    </DialogHeader>

    <div className="space-y-4">
      {/* Banner */}
      <div>
        <label className="block text-sm font-medium mb-2">Banner</label>
        <div className="relative h-32 rounded-xl bg-white/5 overflow-hidden">
          {bannerPreview ? (
            <img src={bannerPreview} alt="Banner" className="w-full h-full object-cover" />
          ) : (
            <div className="w-full h-full bg-gradient-to-br from-purple-500/20 to-blue-500/20" />
          )}
          <label className="absolute inset-0 flex items-center justify-center bg-black/50 opacity-0 hover:opacity-100 cursor-pointer">
            <CameraIcon className="h-8 w-8" />
            <input
              type="file"
              accept="image/*"
              className="hidden"
              onChange={handleBannerChange}
            />
          </label>
        </div>
      </div>

      {/* Name */}
      <div>
        <label className="block text-sm font-medium mb-2">Name</label>
        <Input
          value={name}
          onChange={(e) => setName(e.target.value)}
          maxLength={50}
        />
      </div>

      {/* Bio */}
      <div>
        <label className="block text-sm font-medium mb-2">Bio</label>
        <Textarea
          value={bio}
          onChange={(e) => setBio(e.target.value)}
          maxLength={160}
          rows={3}
        />
        <p className="text-xs text-secondary mt-1">{bio.length}/160</p>
      </div>

      {/* City */}
      <div>
        <label className="block text-sm font-medium mb-2">City</label>
        <Input
          value={city}
          onChange={(e) => setCity(e.target.value)}
          placeholder="Moscow"
        />
      </div>

      {/* Website */}
      <div>
        <label className="block text-sm font-medium mb-2">Website</label>
        <Input
          value={website}
          onChange={(e) => setWebsite(e.target.value)}
          placeholder="https://example.com"
        />
      </div>
    </div>

    <DialogFooter className="mt-6">
      <Button variant="ghost" onClick={() => setIsEditOpen(false)}>
        Cancel
      </Button>
      <Button onClick={handleSave} disabled={isSaving}>
        {isSaving ? 'Saving...' : 'Save Changes'}
      </Button>
    </DialogFooter>
  </DialogContent>
</Dialog>
```

---

## Posts Feed

**Р¤Р°Р№Р»:** `src/pages/Profile.tsx`, `UserProfile.tsx`

### Tabs

```tsx
<div className="border-b border-white/10">
  <div className="flex">
    <button
      className={`flex-1 py-3 text-sm font-medium border-b-2 transition-colors ${
        activeTab === 'posts'
          ? 'border-primary text-white'
          : 'border-transparent text-secondary hover:text-white'
      }`}
      onClick={() => setActiveTab('posts')}
    >
      Posts
    </button>
    <button
      className={`flex-1 py-3 text-sm font-medium border-b-2 transition-colors ${
        activeTab === 'media'
          ? 'border-primary text-white'
          : 'border-transparent text-secondary hover:text-white'
      }`}
      onClick={() => setActiveTab('media')}
    >
      Media
    </button>
    <button
      className={`flex-1 py-3 text-sm font-medium border-b-2 transition-colors ${
        activeTab === 'likes'
          ? 'border-primary text-white'
          : 'border-transparent text-secondary hover:text-white'
      }`}
      onClick={() => setActiveTab('likes')}
    >
      Likes
    </button>
  </div>
</div>
```

### Pinned Post

```tsx
{pinnedPost && (
  <div className="mb-4">
    <div className="flex items-center gap-2 px-6 py-2 text-xs text-secondary">
      <PinIcon className="h-4 w-4" />
      <span>Pinned Post</span>
    </div>
    <Post post={pinnedPost} />
  </div>
)}
```

### Posts List

```tsx
<div className="space-y-4">
  {posts.map((post) => (
    <Post key={post.id} post={post} />
  ))}
</div>

{isLoading && (
  <div className="space-y-4">
    {[1, 2, 3].map((i) => (
      <Skeleton key={i} className="h-48 rounded-[28px]" />
    ))}
  </div>
)}

{posts.length === 0 && !isLoading && (
  <div className="flex flex-col items-center justify-center py-12 text-center">
    <FileTextIcon className="h-16 w-16 text-white/20 mb-4" />
    <p className="text-lg font-medium">No posts yet</p>
    <p className="text-sm text-secondary">
      {isOwn ? 'Start posting to see your content here' : 'This user hasn\'t posted anything yet'}
    </p>
  </div>
)}
```

---

## Follow Modal

**Р¤Р°Р№Р»:** `src/components/profile/FollowModal.tsx`

### РЎС‚СЂСѓРєС‚СѓСЂР°

```tsx
<Dialog open={isOpen} onOpenChange={setIsOpen}>
  <DialogContent>
    <DialogHeader>
      <DialogTitle>
        {type === 'followers' ? 'Followers' : 'Following'}
      </DialogTitle>
    </DialogHeader>

    <div className="space-y-2">
      {users.map((user) => (
        <div
          key={user.id}
          className="flex items-center justify-between p-3 rounded-xl hover:bg-white/5 transition-smooth"
        >
          <div className="flex items-center gap-3">
            <Avatar src={user.avatar} fallback={user.name.charAt(0)} />
            <div className="min-w-0">
              <p className="text-sm font-semibold truncate">{user.name}</p>
              <p className="text-xs text-secondary truncate">@{user.username}</p>
            </div>
          </div>
          {user.id !== currentUserId && (
            <Button
              size="sm"
              variant={user.isFollowing ? 'outline' : 'default'}
              onClick={() => handleFollowToggle(user.id)}
            >
              {user.isFollowing ? 'Following' : 'Follow'}
            </Button>
          )}
        </div>
      ))}
    </div>

    {hasMore && (
      <Button
        variant="ghost"
        className="w-full mt-4"
        onClick={handleLoadMore}
        disabled={isLoadingMore}
      >
        {isLoadingMore ? 'Loading...' : 'Load More'}
      </Button>
    )}
  </DialogContent>
</Dialog>
```

---

## РҐСѓРєРё

### useProfile

```typescript
const { data: user, isLoading } = useQuery({
  queryKey: ['profile', userId],
  queryFn: () => fetchProfile(userId),
});
```

### useUpdateProfile

```typescript
const updateProfile = useMutation({
  mutationFn: (data: ProfileData) => updateProfileApi(data),
  onSuccess: () => {
    queryClient.invalidateQueries({ queryKey: ['profile'] });
    setIsEditOpen(false);
  },
});
```

### useFollow

```typescript
const follow = useMutation({
  mutationFn: (userId: string) => followApi(userId),
  onMutate: async ({ userId }) => {
    await queryClient.cancelQueries({ queryKey: ['profile', userId] });
    const previous = queryClient.getQueryData(['profile', userId]);
    queryClient.setQueryData(['profile', userId], (old) => ({
      ...old,
      isFollowing: true,
      followersCount: old.followersCount + 1,
    }));
    return { previous };
  },
  onError: (err, variables, context) => {
    queryClient.setQueryData(['profile', variables.userId], context.previous);
  },
});
```

### useUserPosts

```typescript
const { data: posts } = useQuery({
  queryKey: ['user', userId, 'posts'],
  queryFn: () => fetchUserPosts(userId),
});
```

---

## РЎРІСЏР·Р°РЅРЅС‹Рµ РґРѕРєСѓРјРµРЅС‚С‹

- [Overview](./OVERVIEW.md) вЂ” РљР°СЂС‚Р° РїСЂРёР»РѕР¶РµРЅРёСЏ
- [Style System](./STYLE_SYSTEM.md) вЂ” UI СЃС‚РёР»СЊ
- [Feed UI](./FEED_UI.md) вЂ” UI Р»РµРЅС‚С‹

